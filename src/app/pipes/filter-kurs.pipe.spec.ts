import { FilterKursPipe } from './filter-kurs.pipe';

describe('FilterKursPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKursPipe();
    expect(pipe).toBeTruthy();
  });
});
